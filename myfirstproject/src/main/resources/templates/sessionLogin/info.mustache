{{>layouts/header}}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>유저 정보</title>
</head>
<body>
<h1>유저 정보</h1>
{{#user}}
    <div>
        <div> PK: {{user.Id}}</div>
        <div> 아이디: {{user.loginId}}</div>
        <div> 닉네임: {{user.nickname}}</div>
        <div> 역할: {{user.role}}</div>
    </div>
{{/user}}
</body>
</html>

{{>layouts/footer}}