{{>layouts/header}}

<table class="table">
    <thead>
    <tr>
        <th scope="col">Id</th>
        <th scope="col">Title</th>
        <th scope="col">Content</th>
    </tr>
    </thead>
    <tbody>
    {{#noticeList}}
        <tr>
            <th>{{id}}</th>
            <td><a href="/notices/{{id}}">{{title}}</a></td>
            <td>{{content}}</td>
        </tr>
    {{/noticeList}}
    </tbody>
</table>

{{>layouts/footer}}
